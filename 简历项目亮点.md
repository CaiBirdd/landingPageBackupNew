# SynapNote AI 会议助手落地页（Vue3 + Vite）

## 项目概述
- 为 SynapNote AI 会议助手打造营销落地页，覆盖 PC/移动端，核心目标是获取潜在用户（邮箱订阅）并收集产品反馈。
- 深度集成数据追踪：UTM 归因 + PostHog 行为分析 + Facebook Pixel 广告转化，支撑广告投放与增长分析。
- 采用 Docker + Nginx 生产部署，提供防爬虫与静态资源优化，保证上线可用性与数据可靠性。

## 个人职责
- 独立完成前端架构与开发，拆分 PC/移动双端视图，封装通用逻辑（数据追踪、API 调用）。
- 设计并落地 UTM 归因与 session 管理方案，打通广告→访问→转化的全链路数据。
- 配置容器化与 Nginx 生产环境，完成防爬虫、缓存与 gzip 优化。

## 关键技术栈
- 前端：Vue 3 + Vite，Composition API，Vant（移动端），SCSS，PostCSS（postcss-pxtorem）
- 数据追踪：PostHog、Facebook Pixel
- 网络：Axios 封装，基于 HTTPS API
- 部署：Docker 多阶段构建，Nginx（防爬虫、缓存、history 路由）

## 核心功能与亮点
- **双端体验**：User-Agent 识别切换 PC/移动端视图；移动端使用 rem 自适应（动态 root font-size + postcss-pxtorem），PC 端保留精细像素布局。
- **用户获取链路**：邮箱订阅与反馈表单，前端校验 + 统一错误处理，成功/失败均有 Toast 提示与埋点记录。
- **UTM 归因 + Session 管理**：解析 URL UTM 参数，后端生成 session_id 存储 localStorage，后续提交均携带 session_id，串联广告来源与转化。
- **埋点体系**：PostHog 记录提交成功/失败/异常，identify 绑定邮箱；Facebook Pixel 触发 Lead 事件，支持投放优化与 ROI 评估。
- **防爬虫与 SEO 控制**：Nginx 基于 User-Agent 黑/白名单拦截主流爬虫和恶意工具，排除 robots.txt，请求异常直接 403。
- **性能与体验**：Gzip + 长缓存（静态资源 immutable），Nginx try_files 兼容前端路由；移动端监听 resize 动态调整 rem，保证可读性。
- **可维护性**：API 模块化封装（createFeedback/recordUtmParams），Composables 封装追踪逻辑（usePosthog/useFbPixel），样式 scoped 隔离。
- **工程化**：Docker 多阶段构建减小镜像体积；Docker Compose 一键启动，暴露 8080→80。

## 可量化/可展示的点（可按需取舍）
- 广告归因：支持 campaign/adset/ad_name 等多维 UTM 追踪，生成 session_id 串联转化。
- 转化埋点：邮箱/反馈提交成功触发 PostHog + Pixel，便于计算 CVR、CPA。
- 体验优化：前端校验减少无效请求，异常路径埋点便于回溯问题。
- 安全与合规：防爬虫降低无效流量，静态资源缓存与 gzip 提升加载速度。

## 简历可用的一句话示例
- 「负责 SynapNote AI 会议助手落地页（Vue3+Vite），搭建 PC/移动双端体验；实现 UTM 归因+session 管理、PostHog/Facebook Pixel 双埋点，打通广告→访问→转化全链路；基于 Docker+Nginx 部署并配置防爬虫/缓存优化。」


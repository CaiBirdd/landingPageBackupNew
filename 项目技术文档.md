# SynapNote 落地页项目技术文档

## 📋 目录

- [项目概述](#项目概述)
- [技术栈](#技术栈)
- [项目结构](#项目结构)
- [核心功能详解](#核心功能详解)
- [技术细节](#技术细节)
- [数据追踪方案](#数据追踪方案)
- [部署方案](#部署方案)
- [开发规范](#开发规范)

---

## 项目概述

### 项目背景

SynapNote 是一个 AI 会议助手产品（Meeting Strategist），本落地页项目用于产品营销和用户获取。落地页的主要目标是：

1. **用户获取**：通过邮箱订阅收集潜在用户信息
2. **用户反馈**：收集用户对产品的意见和建议
3. **广告效果追踪**：通过 UTM 参数追踪不同广告渠道的转化效果
4. **数据埋点**：记录用户行为，用于产品优化和广告投放优化

### 项目特点

- ✅ **响应式设计**：PC 端和移动端完全分离的视图组件，提供最佳用户体验
- ✅ **数据驱动**：完整的数据追踪体系，支持广告效果分析
- ✅ **工程化部署**：Docker 容器化，Nginx 生产环境配置
- ✅ **用户体验优化**：完善的错误处理和用户提示机制

---

## 技术栈

### 前端框架
- **Vue 3.3.4**：采用 Composition API，代码组织更清晰
- **Vite 4.3.9**：快速构建工具，开发体验优秀
- **Vant 4.9.21**：移动端 UI 组件库（仅移动端使用）

### 样式处理
- **SCSS**：CSS 预处理器，支持嵌套、变量等特性
- **PostCSS + postcss-pxtorem**：移动端 rem 适配方案

### HTTP 请求
- **Axios 1.13.2**：HTTP 客户端，封装了请求拦截器和响应拦截器

### 数据追踪
- **PostHog 1.100.0**：用户行为分析工具
- **Facebook Pixel**：广告转化追踪工具

### 用户提示
- **vue-toastification 2.0.0**：PC 端 Toast 提示组件
- **Vant Toast**：移动端 Toast 提示组件

### 部署
- **Docker**：容器化部署
- **Nginx Alpine**：轻量级 Web 服务器

---

## 项目结构

```
synapnote-landingpage/
├── src/
│   ├── apis/                    # API 接口封装
│   │   ├── createFeedback.js    # 提交反馈/邮箱接口
│   │   └── recordUtmParams.js   # 记录 UTM 参数接口
│   ├── assets/                  # 静态资源（图片、SVG）
│   ├── components/              # 公共组件
│   │   ├── Header.vue          # PC 端头部组件
│   │   └── FeedbackForm.vue    # 反馈表单组件（PC端复用）
│   ├── composables/            # 组合式函数
│   │   ├── usePosthog.js       # PostHog 封装
│   │   └── useFbPixel.js       # Facebook Pixel 封装
│   ├── utils/                  # 工具函数
│   │   └── http.js             # Axios 实例封装
│   ├── views/                  # 页面视图
│   │   ├── landingPage.vue     # PC 端落地页
│   │   └── landingPageMobile.vue # 移动端落地页
│   ├── App.vue                 # 根组件（设备检测和路由）
│   ├── main.js                 # 应用入口
│   └── style.css               # 全局样式
├── dist/                       # 构建产物
├── Dockerfile                  # Docker 构建文件
├── docker-compose.yml          # Docker Compose 配置
├── nginx-full.conf            # Nginx 生产配置（含防爬虫）
├── vite.config.js             # Vite 配置
└── package.json               # 项目依赖配置
```

---

## 核心功能详解

### 1. 设备检测与路由

项目采用**完全分离**的 PC 端和移动端视图，通过 User Agent 检测自动切换。

#### 实现方式

```javascript
// App.vue
const detectDevice = () => {
  const userAgent = navigator.userAgent.toLowerCase()
  return /mobile|android|iphone|ipad|phone|blackberry|iemobile|wpdesktop/i.test(userAgent)
}
const isMobile = ref(detectDevice())
```

**设计决策**：
- 不使用响应式媒体查询，而是基于 User Agent 检测
- PC 端和移动端使用完全不同的组件和样式，提供最佳用户体验
- 移动端采用 rem 适配，PC 端采用固定像素值

### 2. 邮箱订阅功能

用户可以在落地页顶部输入邮箱，申请早期测试资格。

#### 功能流程

1. **用户输入邮箱** → 前端验证（非空 + 格式校验）
2. **提交到后端** → `POST /api/v1/feedback/`，携带 `email` 和 `session_id`
3. **成功反馈** → Toast 提示 + 数据埋点（PostHog + Facebook Pixel）
4. **错误处理** → 网络错误提示 + 错误事件记录

#### 邮箱验证逻辑

```javascript
// 非空验证
if (!formData.value.email) {
  toast.warning("Please enter your email address.")
  return
}

// 格式验证（正则表达式）
const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/
if (!emailRegex.test(formData.value.email)) {
  toast.warning("Please enter a valid email address.")
  return
}
```

### 3. 用户反馈收集

页面底部提供可选的反馈输入框，用户可以提交对产品的建议。

#### 功能特点

- **可选功能**：标注 "Optional"，降低用户心理负担
- **实时提交**：支持 Enter 键快速提交
- **数据关联**：通过 `session_id` 关联到同一用户的邮箱和反馈

### 4. UTM 参数追踪

这是项目的**核心功能之一**，用于追踪广告投放效果。

#### UTM 参数说明

UTM（Urchin Tracking Module）参数用于标识用户来源：

- `campaign_name`：广告活动名称
- `adset_name`：广告组名称
- `ad_name`：广告名称
- `ad_content`：广告内容标识
- `landingpage_content`：落地页内容标识
- `landingpage_topic`：落地页主题

#### 实现流程

```javascript
// 1. 从 URL 解析 UTM 参数
const getUrlParams = () => {
  const params = new URLSearchParams(window.location.search)
  const utmParams = { device: getDeviceType() }
  
  // 只添加有值的参数
  if (params.get('campaign_name')) {
    utmParams.campaign_name = params.get('campaign_name')
  }
  // ... 其他参数
  return utmParams
}

// 2. 发送到后端记录
const recordUtmParams = async () => {
  const utmParams = getUrlParams()
  const hasUtmParams = Object.keys(utmParams).length > 1
  
  if (!hasUtmParams) return // 无 UTM 参数则跳过
  
  const res = await recordUtmParamsAPI(utmParams)
  if (res.data?.session_id) {
    // 保存 session_id 用于后续数据关联
    localStorage.setItem('session_id', res.data.session_id)
    formData.value.session_id = res.data.session_id
  }
}
```

#### Session 管理

- **生成时机**：用户首次访问带 UTM 参数的链接时，后端生成唯一 `session_id`
- **存储方式**：`localStorage` 持久化存储
- **数据关联**：所有用户行为（邮箱提交、反馈提交）都携带 `session_id`，后端可以关联分析

**业务价值**：
- 追踪从广告点击 → 落地页访问 → 邮箱提交的完整转化链路
- 分析不同广告渠道、广告组的转化效果
- 优化广告投放策略

### 5. 数据埋点系统

项目集成了两套数据追踪系统，用于不同目的的分析。

#### PostHog - 用户行为分析

**用途**：产品分析和用户行为追踪

**追踪事件**：

1. **feedback_submitted**：用户提交反馈/邮箱
   ```javascript
   posthog.capture('feedback_submitted', {
     email: data.email || '',
     feedback_content: data.content || '',
     session_id: data.session_id,
     submit_type: submitType, // 'email' 或 'content'
     has_feedback: !!data.content,
     has_email: !!data.email,
     feedback_length: data.content ? data.content.length : 0
   })
   ```

2. **feedback_submit_failed**：提交失败（非 201 状态码）

3. **feedback_submit_error**：网络错误或其他异常

**用户识别**：
```javascript
if (data.email) {
  posthog.identify(data.email, {
    email: data.email,
    last_feedback_time: new Date().toISOString()
  })
}
```

#### Facebook Pixel - 广告转化追踪

**用途**：Facebook 广告平台的转化追踪，用于广告优化

**追踪事件**：
```javascript
trackLead({
  content_name: submitType === 'email' ? 'Email Submission' : 'Feedback Submission',
  content_category: 'Lead',
  value: 1.00,
  currency: 'USD'
})
```

**业务价值**：
- Facebook 广告系统可以根据转化数据自动优化广告投放
- 计算广告 ROI（投资回报率）
- 创建相似受众（Lookalike Audience）

---

## 技术细节

### 1. 移动端 Rem 适配方案

移动端采用 **rem + PostCSS 自动转换** 的适配方案。

#### 设计思路

- **设计稿基准**：440px 宽度
- **根字体大小**：在 440px 屏幕下，1rem = 44px（即 rootValue = 44）
- **动态计算**：根据实际屏幕宽度动态调整根字体大小

#### 实现代码

```javascript
// landingPageMobile.vue
const DESIGN_WIDTH = 440
const ROOT_VALUE_AT_DESIGN_WIDTH = 44

const setRem = () => {
  // 限制最大宽度不超过设计稿宽度
  const viewportWidth = Math.min(
    document.documentElement.clientWidth || document.body.clientWidth,
    DESIGN_WIDTH
  )
  
  // 计算公式：根字体 = (当前视口宽度 / 设计稿宽度) * 设计稿下的 rootValue
  const rootFontSize = (viewportWidth / DESIGN_WIDTH) * ROOT_VALUE_AT_DESIGN_WIDTH
  
  document.documentElement.style.fontSize = `${rootFontSize}px`
}

onMounted(() => {
  setRem()
  window.addEventListener('resize', setRem)
})

onBeforeUnmount(() => {
  window.removeEventListener('resize', setRem)
})
```

#### PostCSS 配置

```javascript
// vite.config.js
pxtorem({
  rootValue: 44, // 设计稿 440px / 10 = 44
  unitPrecision: 5,
  propList: ['*'], // 转换所有 CSS 属性中的 px
  selectorBlackList: [
    'ignore-',
    'header-section-pc',
    'landing-content-pc',
    // ... PC 端组件不转换
  ],
  replace: true,
  mediaQuery: false,
  minPixelValue: 1 // 小于等于 1px 的不转换
})
```

**优势**：
- 开发时使用 px 单位，符合设计习惯
- 自动转换为 rem，适配不同屏幕
- PC 端组件通过黑名单排除，保持固定像素值

### 2. PC 端复杂布局实现

PC 端采用**绝对定位 + 相对定位**的组合布局，实现复杂的视觉设计。

#### 布局结构

```scss
.features-wrapper {
  position: relative; // 相对定位容器
  height: 518px;
  
  .features-card {
    position: absolute; // 功能卡片
    left: 216px;
    top: 0;
    width: 796px;
  }
  
  .pain-point-bubble {
    position: absolute; // 痛点气泡
    // 每个气泡都有精确的 left 和 top 值
  }
  
  .feature-img {
    position: absolute; // 功能图片
    // 6 张图片的精确定位
  }
  
  .gradient-blob {
    position: absolute; // 渐变装饰背景
    z-index: -1;
    filter: blur(100px); // 模糊效果
  }
}
```

**设计亮点**：
- 使用 CSS `filter: blur()` 实现渐变背景的模糊效果
- 通过 `z-index` 控制层级关系
- 所有元素相对于 `features-wrapper` 绝对定位，便于整体缩放

### 3. HTTP 请求封装

使用 Axios 封装统一的 HTTP 客户端。

#### 封装代码

```javascript
// utils/http.js
import axios from 'axios'

const http = axios.create({
  baseURL: 'https://api.synapnote.ai',
  timeout: 5000
})

// 请求拦截器
http.interceptors.request.use(config => {
  return config
}, error => {
  return Promise.reject(error)
})

// 响应拦截器
http.interceptors.response.use(response => {
  return response
}, error => {
  return Promise.reject(error)
})

export default http
```

#### API 接口封装

```javascript
// apis/createFeedback.js
import http from '../utils/http'

export const createFeedbackAPI = (data) => http.post('/api/v1/feedback/', data)

// apis/recordUtmParams.js
export const recordUtmParamsAPI = (data) => http.post('/api/v1/utm/track', data)
```

**优势**：
- 统一的基础 URL 配置
- 统一的超时时间设置
- 便于后续添加请求头、错误处理等逻辑

### 4. Composables 模式应用

使用 Vue 3 Composition API 的 Composables 模式，封装可复用的逻辑。

#### PostHog 封装

```javascript
// composables/usePosthog.js
import posthog from 'posthog-js'

export function usePostHog() {
  posthog.init('phc_FrYqhNKgW7LuRGnIZEnOdoFMIAtMlXtyEtVa8BmChym', {
    api_host: 'https://us.i.posthog.com',
    defaults: '2025-05-24',
  })
  
  return { posthog }
}
```

#### Facebook Pixel 封装

```javascript
// composables/useFbPixel.js
export function useFbPixel() {
  const isFbqAvailable = () => {
    return typeof window !== 'undefined' && typeof window.fbq === 'function'
  }
  
  const track = (eventName, params = {}) => {
    if (isFbqAvailable()) {
      window.fbq('track', eventName, params)
    }
  }
  
  const trackLead = (params = {}) => track('Lead', params)
  
  return {
    track,
    trackLead,
    // ... 其他方法
  }
}
```

**优势**：
- 逻辑复用：多个组件可以共享同一套数据追踪逻辑
- 易于测试：可以单独测试 Composables 函数
- 代码组织清晰：业务逻辑和 UI 逻辑分离

### 5. 错误处理机制

项目实现了完善的错误处理机制，提升用户体验。

#### 错误处理层次

1. **前端验证**：提交前进行格式验证，避免无效请求
2. **网络错误捕获**：使用 try-catch 捕获异步错误
3. **用户提示**：使用 Toast 组件友好提示用户
4. **错误埋点**：记录错误事件到 PostHog，便于问题排查

```javascript
const sendData = async (data, submitType) => {
  try {
    const res = await createFeedbackAPI(data)
    
    if (res.status === 201) {
      // 成功处理
      toast.success("Email submission successful! Thank you.")
      // 埋点
      posthog.capture('feedback_submitted', { ... })
    } else {
      // 非 201 状态码
      toast.warning("Submission failed, please check your network.")
      posthog.capture('feedback_submit_failed', { ... })
    }
  } catch (error) {
    // 网络错误或其他异常
    toast.warning("Submission failed, please check your network.")
    posthog.capture('feedback_submit_error', {
      error_message: error.message
    })
  }
}
```

---

## 数据追踪方案

### 数据流图

```
用户访问落地页
    ↓
[UTM 参数检测]
    ↓
[发送 UTM 参数到后端] → 生成 session_id → 存储到 localStorage
    ↓
用户提交邮箱/反馈
    ↓
[携带 session_id 发送到后端]
    ↓
[PostHog 埋点] → 用户行为分析
    ↓
[Facebook Pixel 埋点] → 广告转化追踪
```

### 数据关联逻辑

1. **首次访问**：用户通过广告链接访问，URL 包含 UTM 参数
2. **Session 生成**：后端根据 UTM 参数生成唯一 `session_id`
3. **Session 存储**：前端将 `session_id` 存储到 `localStorage`
4. **数据关联**：用户后续的邮箱提交、反馈提交都携带 `session_id`
5. **数据分析**：后端可以通过 `session_id` 关联分析：
   - 哪个广告渠道带来了用户
   - 用户从访问到提交的完整行为链路
   - 不同广告的转化率对比

### 埋点事件清单

| 事件名称 | 触发时机 | 追踪平台 | 业务价值 |
|---------|---------|---------|---------|
| `feedback_submitted` | 用户提交邮箱/反馈成功 | PostHog | 分析用户转化行为 |
| `feedback_submit_failed` | 提交失败（非 201） | PostHog | 监控接口异常 |
| `feedback_submit_error` | 网络错误 | PostHog | 监控网络问题 |
| `Lead` | 用户提交邮箱/反馈成功 | Facebook Pixel | 广告转化追踪 |

---

## 部署方案

### Docker 容器化部署

项目采用 **多阶段构建** 的 Docker 镜像，优化镜像大小。

#### Dockerfile 解析

```dockerfile
# 第一阶段：构建阶段
FROM node:18-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

# 第二阶段：生产阶段
FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY robots.txt /usr/share/nginx/html/robots.txt
COPY nginx-full.conf /etc/nginx/nginx.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

**优势**：
- **镜像体积小**：最终镜像只包含 Nginx 和构建产物，不包含 Node.js 和依赖
- **构建缓存优化**：先复制 `package.json`，利用 Docker 缓存层
- **安全性**：使用 Alpine Linux，基础镜像更小更安全

### Nginx 配置详解

#### 1. 防爬虫机制

项目实现了完善的防爬虫机制，阻止搜索引擎和恶意爬虫访问。

```nginx
# User-Agent 黑白名单检测
map $http_user_agent $blocked_agent {
    default 0;
    
    # 黑名单：搜索引擎爬虫
    ~*googlebot 1;
    ~*bingbot 1;
    ~*baiduspider 1;
    # ... 更多爬虫
    
    # 白名单：Facebook 爬虫（用于广告预览）
    ~*facebookexternalhit 0;
    
    # 白名单：正常浏览器
    ~*mozilla 0;
    ~*chrome 0;
    # ...
}

# 空 User-Agent 检测
map $http_user_agent $empty_ua {
    default 0;
    "" 1;
}

# 阻止黑名单和空 UA
if ($check_ua = "10") { return 403; }
if ($check_ua = "01") { return 403; }
if ($check_ua = "11") { return 403; }
```

**业务价值**：
- 防止 SEO 索引，保护落地页的广告追踪数据
- 减少无效流量，节省服务器资源
- 防止恶意爬虫攻击

#### 2. 静态资源优化

```nginx
# Gzip 压缩
gzip on;
gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

# 静态资源缓存
location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
}
```

**优化效果**：
- 减少传输体积，提升加载速度
- 长期缓存静态资源，减少重复请求

#### 3. Vue Router History 模式支持

```nginx
location / {
    try_files $uri $uri/ /index.html;
}
```

确保前端路由正常工作，所有路由都返回 `index.html`。

### Docker Compose 配置

```yaml
services:
  synapnote-landingpage:
    image: synapnote-landingpage:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: synapnote-landingpage
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - synapnote-network

networks:
  synapnote-network:
    driver: bridge
```

**部署命令**：
```bash
# 构建并启动
docker-compose up -d --build

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down
```

---

## 开发规范

### 代码组织原则

1. **组件化**：按功能拆分组件，提高复用性
2. **Composables 模式**：业务逻辑封装为 Composables，便于复用和测试
3. **API 封装**：统一封装 API 接口，便于维护
4. **样式隔离**：使用 `scoped` 样式，避免样式污染

### 命名规范

- **组件**：PascalCase（如 `FeedbackForm.vue`）
- **Composables**：camelCase，以 `use` 开头（如 `usePosthog.js`）
- **API 函数**：camelCase，以 `API` 结尾（如 `createFeedbackAPI`）
- **CSS 类名**：kebab-case（如 `email-section-pc`）

### 注释规范

- **函数注释**：使用 JSDoc 格式，说明参数和返回值
- **复杂逻辑**：添加行内注释，解释设计决策
- **TODO 标记**：使用注释标记待优化点

### 错误处理

- **前端验证**：提交前进行数据验证
- **错误捕获**：使用 try-catch 捕获异步错误
- **用户提示**：使用 Toast 组件友好提示
- **错误埋点**：记录错误事件，便于问题排查

---

## 项目亮点总结

### 技术亮点

1. ✅ **响应式设计**：PC 端和移动端完全分离，提供最佳用户体验
2. ✅ **Rem 适配方案**：移动端采用动态 rem 计算，适配不同屏幕
3. ✅ **数据追踪体系**：完整的 UTM 参数追踪 + 双平台埋点
4. ✅ **Session 管理**：通过 session_id 关联用户完整行为链路
5. ✅ **工程化部署**：Docker 多阶段构建 + Nginx 生产配置
6. ✅ **防爬虫机制**：完善的 User-Agent 检测，保护数据安全

### 业务价值

1. 📊 **广告效果分析**：通过 UTM 参数追踪不同广告渠道的转化效果
2. 📈 **用户行为分析**：PostHog 埋点提供详细的用户行为数据
3. 💰 **广告优化**：Facebook Pixel 数据用于广告投放优化
4. 🔒 **数据安全**：防爬虫机制保护落地页数据不被索引

### 可扩展性

- **组件化设计**：易于添加新功能模块
- **Composables 模式**：业务逻辑易于复用和测试
- **API 封装**：便于对接新的后端接口
- **Docker 部署**：易于横向扩展和 CI/CD 集成

---

## 总结

这是一个**生产级别的落地页项目**，不仅实现了基本的用户获取功能，还建立了完整的数据追踪体系。项目在技术实现、用户体验、数据分析和工程化部署等方面都有很好的实践，是一个值得学习和参考的前端项目。

**核心价值**：
- 为产品获取早期用户
- 为广告投放提供数据支持
- 为产品优化提供用户反馈

**技术价值**：
- 展示了 Vue 3 Composition API 的最佳实践
- 展示了响应式设计和移动端适配方案
- 展示了数据追踪和埋点的完整实现
- 展示了 Docker 容器化部署的工程化实践

